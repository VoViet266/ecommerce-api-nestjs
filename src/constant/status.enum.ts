export enum OrderStatus {
  pending = 'pending',
  shipped = 'shipped',
  delivered = 'delivered',
  cancelled = 'cancelled',
}
export enum PaymentStatus {
  pending = 'pending',
  paid = 'paid',
  failed = 'failed',
}
